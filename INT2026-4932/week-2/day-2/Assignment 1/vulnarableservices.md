➤ Running Services

Services such as FTP, Telnet, HTTP, SSH, and MySQL are running on the target machine.

➤ Possible Vulnerable Services

FTP (vsftpd 2.3.4) – Known backdoor vulnerability

Telnet – Transmits credentials in plain text

HTTP (Apache) – May allow web exploitation

MySQL – Weak credentials possible